[project]
name = "latentum"
version = "0.1.0"
description = "LatentUM"
readme = "README.md"
requires-python = "==3.12.0"
dependencies = [
    "accelerate>=1.12.0",
    "diffusers",
    "einops>=0.8.1",
    "flash-attn==2.8.3",
    "omegaconf>=2.3.0",
    "pillow>=12.0.0",
    "safetensors>=0.6.2",
    "timm>=1.0.24",
    "torch==2.8.0",
    "torchvision==0.23.0",
    "tqdm>=4.67.1",
    "transformers>=4.57.3",
]

[tool.uv.extra-build-dependencies]
flash-attn = ["torch==2.8.0"]
